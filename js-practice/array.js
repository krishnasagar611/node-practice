
let arr = [10, 0, 4, 2, 3, 2]
// array push  push element in the last 
// arr.push(6,9)
// remove the last element
// arr.pop()
//add a element in the 1st 
// arr.unshift(10)
// shift remove the 1st element 
// arr.shift()
//include methode will check that perticular element is present in the array or not 
//  cnsole.log(arr.includes(4));
//indexof methode will give that perticular element is present or it (if present it will print that element if not give -1)
// console.log(arr.indexOf(1))

// join methode convert the array element to string 

// console.log(arr.join())  

//slice methode does not  includes last and element which we are giving inside  the  slice methode

// console.log(arr.slice(1,3))

//splice methode

console.log(arr.splice(1,3))


